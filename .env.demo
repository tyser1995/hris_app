# Demo / presentation mode — uses in-memory mock data, no real Supabase calls.
# Run with: flutter run -d chrome --dart-define=ENV_FILE=.env.demo
SUPABASE_URL=https://demo-placeholder.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
MOCK_DATA=true
